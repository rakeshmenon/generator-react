{
  "name": "<%= _.slugify(appname) %>",
  "version": "0.0.0",
  "dependencies": {<% if (requireJS) { %>
    "requirejs": "latest",
    "almond": "latest",<% } %><% if (includeModernizr) { %>
    "modernizr": "latest",<% } %>
    "react": "latest"
  },
  "devDependencies": {}
}
